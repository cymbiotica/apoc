function Meepple () {
    const _player = type // player(1 or 2?)
    const _color = color // blue, yellow, green
    let _state = state // stand, knockedDown, sacrificed
  }

  Meeple.prototype.getState = function () {
    return this._state
  }
